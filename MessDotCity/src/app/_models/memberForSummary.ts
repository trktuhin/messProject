export interface MemberForSummary {
    memberId: number;
    firstName: string;
    lastName: string;
    userId: number; // nullable
    photoUrl: string;
    deposit: number;
    totalMeal: number;
}
